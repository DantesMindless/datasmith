import React from "react";
import Button from '@mui/joy/Button'

export default function Connection (type: string):React.ReactElement {
    return (
        <div>
            <Button>Button</Button>
        </div>
    )
}
